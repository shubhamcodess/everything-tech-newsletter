# Prompt to paste into the routine's "Instructions" box

Paste the text below (between the lines) at
claude.ai/code/routines/new. Kept short on purpose — the actual pipeline
lives in this repo's `CLAUDE.md`, which loads automatically once the repo
is attached, so the routine's prompt only needs to point at it.

---

Follow CLAUDE.md's steps 1–11 exactly, in order, for today's run. Before
writing any story summary in step 8, read the "Writing digest summaries"
section and follow it exactly -- summary quality is what this digest is
judged on, don't default to generic filler. Don't read or explore
anything else in the repo first. Stop after step 11's one-line summary.

---
